language: node_js

node_js:
  - "0.12"
  - "4.0"
  - "4"
  - "stable"

env:
  - CXX=g++-4.8

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8

script:
  - npm run lint
  - npm test
